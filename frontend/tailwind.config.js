export const darkMode = 'class';
export const content = [
    "./src/**/*.{js,jsx,ts,tsx}", // Ensure all components are scanned for Tailwind classes
];
export const theme = {
    extend: {},
};
export const plugins = [];
  